# 21단계 — iOS/macOS 검증 기반

`BuildIosXcode`와 macOS self-hosted workflow가 Unity 테스트, Xcode export와 무서명
`xcodebuild` 컴파일을 수행합니다. 현재 Windows PC에는 Unity iOS 모듈, macOS와 Xcode가
없어 실제 실행하지 못했습니다. Apple signing secret은 저장소에 포함하지 않습니다.

